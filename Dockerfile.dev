# Development Dockerfile - installs dependencies only
# Source code is mounted as volumes for hot reloading
#
# Used by docker-compose.dev-full.yml for fully containerized development

FROM node:22-alpine

WORKDIR /app

# Install frontend dependencies
# Note: After adding/updating dependencies, rebuild with: docker compose -f docker-compose.dev-full.yml up --build
COPY package*.json ./
RUN npm ci

# Install server dependencies
COPY server/package*.json ./server/
RUN cd server && npm ci

# Source code will be mounted as volumes - not copied here
# This enables hot reloading without rebuilding the container

# Default command (overridden per-service in docker-compose)
CMD ["npm", "run", "dev"]
