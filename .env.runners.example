# GitHub Actions Self-Hosted Runners Environment Variables
#
# Copy this file to /opt/github-runners/.env and fill in your values
#
# To create a Personal Access Token:
#   1. Go to GitHub Settings → Developer settings → Personal access tokens → Fine-grained tokens
#   2. Create a token for the deadonfilm repository
#   3. Grant "Administration" permission (read and write) under Repository permissions
#   4. Copy the token value below
#
# Note: Use ACCESS_TOKEN (not GITHUB_ACCESS_TOKEN) - this is what the
# myoung34/github-runner image expects

# Required - GitHub Personal Access Token with repo admin permissions
ACCESS_TOKEN=ghp_your_personal_access_token_here

# Required - Full URL to your repository
REPO_URL=https://github.com/chenders/deadonfilm

# Optional - Runner group (for organization-level runners)
# RUNNER_GROUP=default

# Optional - Alternative: Use ORG_NAME instead of REPO_URL for org-level runners
# ORG_NAME=your-org-name
