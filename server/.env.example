# TMDB API Token (required)
# Get yours at https://www.themoviedb.org/settings/api
TMDB_API_TOKEN=your_tmdb_api_token_here

# Anthropic API Key (required for accurate cause of death lookups)
# Get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Server port (optional, defaults to 8080)
PORT=8080

# PostgreSQL connection string (optional for local dev, required for Docker)
# For Docker Compose, this is set automatically
# DATABASE_URL=postgresql://deadonfilm:deadonfilm@localhost:5432/deadonfilm

# Redis connection (optional - caching disabled if not set)
# For Docker Compose, this is set automatically
# REDIS_URL=redis://localhost:6379

# Google Search API Key (optional - improves cause of death lookups)
# Setting Up Google Custom Search API
#
# Step 1: Enable the API
# 1. Go to https://console.cloud.google.com/
# 2. Select or create a project
# 3. Go to APIs & Services → Library
# 4. Search for "Custom Search API" and enable it
# Step 2: Create an API Key (not service account)
# 1. Go to APIs & Services → Credentials
# 2. Click Create Credentials → API Key
# 3. Copy the key - this is your GOOGLE_SEARCH_API_KEY
# 4. (Optional) Restrict it to only Custom Search API for security
# Step 3: Create a Programmable Search Engine
# 1. Go to https://programmablesearchengine.google.com/
# 2. Click Add to create a new search engine
# 3. For "Sites to search", enter * (or leave blank and check "Search the entire web")
# 4. Give it a name, click Create
# 5. Go to the control panel and copy the Search engine ID - this is your GOOGLE_SEARCH_CX
# GOOGLE_SEARCH_API_KEY=AIza...your-api-key
# GOOGLE_SEARCH_CX=a1b2c3...your-search-engine-id

# Bing Search API Key (optional - additional search fallback)
# Pricing: Free tier (F0) = 1000 queries/month, S1 = $3/1000 queries
#
# Setup:
# 1. Go to https://portal.azure.com/
# 2. Click "Create a resource" → search "Bing Search v7"
# 3. Select "Bing Search" → Create
# 4. Choose F0 (Free) pricing tier
# 5. Once deployed, go to "Keys and Endpoint" and copy Key 1
# BING_SEARCH_API_KEY=your-bing-api-key

# The Guardian API Key (optional - UK news obituaries)
# Pricing: Free for non-commercial use (12 calls/second, 5000 calls/day)
#
# Setup:
# 1. Go to https://open-platform.theguardian.com/access/
# 2. Register for a developer key
# 3. Copy your API key
# GUARDIAN_API_KEY=your-guardian-api-key

# New York Times API Key (optional - obituaries and death news)
# Pricing: Free (500 requests/day, 5 requests/minute)
#
# Setup:
# 1. Go to https://developer.nytimes.com/
# 2. Create an account and app
# 3. Enable "Article Search API"
# 4. Copy your API key
# NYTIMES_API_KEY=your-nytimes-api-key

# FamilySearch API Key (optional - genealogical death records)
# Pricing: Free (1000 calls/day)
# Best for historical records (pre-1990s deaths)
#
# Setup:
# 1. Go to https://www.familysearch.org/developers/
# 2. Register for a developer account
# 3. Create an app and get API key
# FAMILYSEARCH_API_KEY=your-familysearch-api-key
