#!/bin/sh

# CI runs type-check, lint, format-check, and tests on every PR.
# Pre-commit (lint-staged) already catches lint/format issues at commit time.
# No additional checks needed here — avoid slowing down push.
